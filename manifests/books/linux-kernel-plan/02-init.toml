part_id = "INIT"
order = 2
title = "通用初始化、initcall 与用户空间启动"

[[chapter]]
id = "LK-INIT-001"
order = 5
question = "start_kernel() 接手时哪些执行条件已经成立？"
outcome = "区分 ARM64 汇编阶段完成的工作与通用初始化仍需建立的子系统"
prerequisites = ["LK-BOOT-004"]
evidence = ["fixed symbols", "boot checkpoints", "dependency table"]
status = "planned"

[[chapter]]
id = "LK-INIT-002"
order = 6
question = "rest_init() 如何创建 kernel_init 和 kthreadd？"
outcome = "追踪启动主线程如何建立后续内核线程与用户空间启动路径"
prerequisites = ["LK-INIT-001"]
evidence = ["fixed symbols", "process or boot trace", "thread identity output"]
status = "planned"

[[chapter]]
id = "LK-INIT-003"
order = 7
question = "initcall 宏如何把函数注册进内核镜像？"
outcome = "从宏展开追踪到专用 linker section"
prerequisites = ["LK-INIT-001"]
evidence = ["macro expansion", "linker symbols", "ELF section inspection"]
status = "planned"

[[chapter]]
id = "LK-INIT-004"
order = 8
question = "不同级别的 initcall 如何在链接阶段形成有序区间？"
outcome = "从 linker script 判断各级 initcall 的排列和边界"
prerequisites = ["LK-INIT-003"]
evidence = ["linker script", "ELF section order", "boundary symbols"]
status = "planned"

[[chapter]]
id = "LK-INIT-005"
order = 9
question = "do_initcalls() 如何找到并调用各级 initcall？"
outcome = "连接 linker section、边界指针和运行时调用循环"
prerequisites = ["LK-INIT-004"]
evidence = ["fixed symbols", "function trace", "boot log"]
status = "planned"

[[chapter]]
id = "LK-INIT-006"
order = 10
question = "initcall_debug 如何验证 initcall 的顺序和耗时？"
outcome = "用内核参数和日志验证 initcall 源码路径"
prerequisites = ["LK-INIT-005"]
evidence = ["kernel command line", "real dmesg output", "timing comparison"]
status = "planned"

[[chapter]]
id = "LK-INIT-007"
order = 11
question = "kernel_init 如何选择并执行用户空间 init？"
outcome = "追踪内核线程如何尝试 init 路径并进入第一个用户态程序"
prerequisites = ["LK-INIT-002", "LK-INIT-005"]
evidence = ["fixed symbols", "initramfs fixture", "boot output"]
status = "planned"
