项目状态
========

最后更新
--------

2026-07-11

当前阶段
--------

``control-file-inventory``：仓库已经确定 ``aiBook`` 的 Roadmap 和文章生成控制文件负责表达用户想获得的知识，旧 ``docs`` 只作为失败生成结果。目前正在盘点各路径的控制文件和尚未整理的知识项。

当前结论
--------

* 仓库只在 ``main`` 工作；
* ``AGENTS.md`` 是所有新 Agent 的第一入口；
* 项目规则、学习路径和正式内容以 RST 为主；
* 少量 TOML 只记录控制文件来源、知识覆盖、路径、单元和进度；
* RST 源文件就是最终阅读内容；
* 仓库不使用 Python 工具、Sphinx、GitHub Actions、CI、构建、release 脚本或独立 Skill；
* ``aiBook`` Roadmap 和生成控制文件定义需要学习的知识范围；
* Roadmap 条目是覆盖要求，不是一条一章；
* 技术事实重新来自官方资料、固定源码和真实可确认的行为；
* 旧 ``aiBook/docs`` 只用于分析过度扩充、前置缺失和教学失败；
* 当前不从旧正文直接改写，也不批量生成新内容；
* Linux 现有四阶段为暂定设计，Roadmap 盘点后才能确认。

已经完成
--------

* 建立跨对话 ``AGENTS.md``、状态、决策和文件职责记录；
* 总结 ``aiBook`` 的主要教学失败并形成学习主链；
* 删除 Linux 固定 17 章计划与旧 Skill；
* 删除 Python 校验脚本、``pyproject.toml``、Sphinx、GitHub Actions、CI、release 和独立 lab 层；
* 将仓库精简为直接阅读的 RST 内容仓库；
* 明确区分 Roadmap/控制文件、技术事实来源和旧 ``docs`` 三种资产；
* 将 Roadmap 定义为知识覆盖输入，而不是章节目录；
* 规定知识项可以合并、拆分、调整顺序、延后或明确排除；
* 在 unit 状态和 RST 模板中增加 Roadmap 覆盖与事实来源记录；
* 将七条路径状态统一改为 ``control-inventory-pending``；
* 将 Linux 路径阶段标记为 Roadmap 盘点前的暂定设计。

当前问题
--------

* 尚未定位并登记七条路径各自的 Roadmap 和文章生成控制文件路径；
* 尚未从控制文件中提取完整知识库存；
* 尚未区分哪些控制条目是真实知识需求，哪些只服务于旧文章生成结构；
* 还没有经过实际阅读验证的黄金学习单元；
* unit 的篇幅、解释密度和评估难度尚未由真实样例校准；
* 当前 unit 状态格式还没有经过真实内容使用。

下一阶段
--------

#. 在 ``aiBook`` 中定位每条路径对应的 Roadmap、规范和文章生成控制文件；
#. 将准确路径写入 ``manifests/tracks.toml`` 和对应 track TOML；
#. 从控制文件提取用户真正想获得的知识项；
#. 标记重复、过大、前置缺失和只服务文章结构的条目；
#. 根据依赖关系形成路径知识库存，而不是章节目录；
#. 从库存中选择语言基础、系统行为和源码阅读三个黄金学习单元；
#. 使用一手资料和真实行为重新建立内容；
#. 实际阅读并记录哪里仍然不清楚。

状态维护规则
------------

每次完成有意义的工作后，更新“已经完成”“当前问题”和“下一阶段”。长期方向写入 ``project/DECISIONS.rst``；文件职责写入 ``project/REPOSITORY_MAP.rst``；Roadmap 来源和知识覆盖写入 track 或 unit TOML。
