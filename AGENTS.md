# AGENTS.md

## 1. 仓库使命

`cxy-251/techBook` 把 `cxy-251/aiBook` 的知识规划重新做成面向零基础程序员、可以直接阅读的 RST 学习资料。

`aiBook` 中的 Roadmap 和文章生成控制文件表达“用户希望获得哪些知识”；旧 `docs` 是按这些控制文件生成出来的成品，不能直接作为新内容底稿。

目标是让读者逐步做到：

1. 运行或观察一个具体例子；
2. 在看到解释前预测结果；
3. 根据代码、对象、状态或输出解释原因；
4. 改变条件后重新判断；
5. 识别常见错误模型；
6. 把理解迁移到未见过的问题。

## 2. 内容依据

新内容使用三层依据，职责不能混淆。

### 2.1 知识范围

`aiBook` 的 Roadmap、Roadmap 配套规范和文章生成控制文件决定：

- 需要学习的主题；
- 主题之间原本希望覆盖的关系；
- 用户对深度、工程性和源码层级的要求。

它们是知识需求清单，不是新仓库的章节目录。

### 2.2 技术事实

所有事实、代码行为、源码路径和工程结论必须重新来自：

- 官方文档、标准与规范；
- 固定版本的真实源码；
- 可以确认的代码、命令、输出、日志或系统现象；
- 当前主题可靠的一手资料。

Roadmap 只规定需要学什么，不能证明技术结论本身。

### 2.3 旧正文

`aiBook/docs` 只用于观察旧生成结果的问题，例如：

- 哪些主题被扩充得过多；
- 哪些前置知识缺失；
- 哪些问题与正文脱节；
- 哪些解释看似完整，读完仍无法形成判断。

旧正文默认不复制、不缩写、不逐章改写。旧正文中的链接、代码、源码路径和结论也必须重新核实。

## 3. Roadmap 的使用规则

Roadmap 中的知识项属于“覆盖要求”，不等于“一项一章”。

允许根据零基础学习顺序：

- 合并重复或高度相关的知识项；
- 把一个过大的知识项拆成多个能力台阶；
- 调整顺序，使前置能力先建立；
- 删除为了文章形式而产生的重复展开；
- 延后源码和工程细节，先建立行为模型。

不得在没有记录的情况下丢弃 Roadmap 中用户真正想学的知识。每条路径需要在入口 RST 或状态 TOML 中记录知识项当前属于：待设计、已合并、已覆盖、延后或明确排除，并说明原因。

## 4. 仓库形式

仓库采用轻量 RST 内容形式：

- `AGENTS.md` 保存最高执行规则；
- 项目说明、教学规则、学习路径和正式内容使用 reStructuredText；
- 少量 TOML 只用于记录路径、知识覆盖、单元和进度状态；
- 代码、命令、输出和源码片段直接写入对应 RST；
- 不建立 Python 工具、`pyproject.toml`、Sphinx、GitHub Actions、CI、构建、发布脚本或独立 Skill；
- RST 源文件就是最终可阅读内容。

不得因为未来可能需要某项功能而提前创建自动化骨架。

## 5. 新对话接手顺序

开始任何工作前依次读取：

1. `AGENTS.md`；
2. `README.rst`；
3. `project/STATE.rst`；
4. `project/DECISIONS.rst`；
5. `project/LEARNING_DESIGN.rst`；
6. `project/AIBOOK_LESSONS.rst`；
7. `project/CONTENT_SELECTION.rst`；
8. `project/REPOSITORY_MAP.rst`；
9. `manifests/tracks.toml`；
10. 当前路径的 Roadmap/控制文件库存、路径状态与学习单元状态；
11. 最近相关 RST 与 `main` 最近提交。

不得依赖上一段聊天的隐含记忆。仓库文件是跨对话、跨助手的交接依据。

## 6. 当前阶段

仓库处于知识控制文件盘点阶段。

在正式生成黄金学习单元前，先为目标路径确认：

- `aiBook` 中对应的 Roadmap 和生成控制文件；
- Roadmap 中真正想获得的知识项；
- 哪些条目只是生成结构要求；
- 哪些知识项被旧正文过度扩充；
- 新内容需要重新查证的一手来源类型。

## 7. 分支规则

- 只在 `main` 分支工作；
- 不创建功能分支和 PR，除非用户明确改变规则；
- 每次工作围绕一个清晰目标；
- 不运行或配置 GitHub Actions。

## 8. 内容规划

学习路径开始时固定：

- 目标读者；
- 入口能力；
- 最终能力；
- Roadmap 知识覆盖范围；
- 阶段顺序；
- 明确排除项。

不在开头冻结整条路径的全部章节。每个阶段开始前，从 Roadmap 知识项中设计一个小批次学习单元；每次处理当前批次中顺序最靠前的未完成项。批次完成后根据真实阅读反馈设计下一批。

## 9. 学习单元要求

一个正式学习单元至少需要形成以下主链：

1. 说明读者为什么现在会遇到这个问题；
2. 在解释前要求读者预测；
3. 给出最小代码、命令、输出、错误、界面现象或源码证据；
4. 逐步连接输入、调用、对象、状态和结果；
5. 改变至少一个条件并重新判断；
6. 指出一个常见错误模型及其错误原因；
7. 设计来自前述现象的能力检查；
8. 给出答案的推理过程，也说明常见错误答案为什么错；
9. 给出一个不能直接照抄原例子的迁移任务；
10. 用读者能独立完成的动作定义完成标准。

这是一条学习主链，不是强制标题模板。

## 10. RST 内容规则

- 一个文件只承担一次清晰的能力变化；
- 在文件开头记录它覆盖的 Roadmap 知识项；
- 代码直接使用 `.. code-block::` 写在 RST 中；
- 命令和输出直接使用 `.. code-block:: console` 写在 RST 中；
- 源码级内容记录 repository、release、commit、平台和工具链；
- 实际输出标明验证环境和验证日期；未实测内容必须明确写成预期结果；
- 不使用 `literalinclude`、Sphinx 专属交叉引用或依赖构建才能工作的结构；
- 使用普通 RST 相对链接连接文件；
- 不写宏大开场、重复总结、百科式分类和与正文脱节的问题；
- 不设置最低字数；能力变化完成后结束。

## 11. 工作结束记录

每次完成有意义的工作后，至少更新以下一项：

- `project/STATE.rst`：完成内容、当前问题和下一步；
- `project/DECISIONS.rst`：新的长期决定及原因；
- `project/REPOSITORY_MAP.rst`：文件职责变化；
- track 或 unit TOML：Roadmap 来源、知识覆盖、阶段、单元和进度变化。
