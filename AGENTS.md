# AGENTS.md

## 1. 仓库使命

`cxy-251/techBook` 把 `cxy-251/aiBook` 中 `docs` 的技术方向重做为面向零基础程序员的可直接阅读学习资料。

目标不是生成更多文章，而是让读者逐步做到：

1. 运行或观察一个具体例子；
2. 在看到解释前预测结果；
3. 根据代码、对象、状态或输出解释原因；
4. 改变条件后重新判断；
5. 识别常见错误模型；
6. 把理解迁移到未见过的问题。

## 2. 仓库形式

仓库采用与 `paperToVideo` 相同的轻量形式：

- `AGENTS.md` 保存最高执行规则；
- 项目说明、教学规则、学习路径和正式内容使用 reStructuredText（RST）；
- 少量 TOML 只用于记录路径、单元和进度状态；
- 代码、命令、输出和源码片段直接写入对应 RST；
- 不建立 Python 工具、`pyproject.toml`、Sphinx、GitHub Actions、CI、构建、发布脚本或独立 Skill；
- RST 源文件就是最终可阅读内容，不要求转换为 HTML。

不得因为未来可能需要某项功能而提前创建自动化骨架。真实需求出现后再决定是否增加。

## 3. 新对话接手顺序

开始任何工作前依次读取：

1. `AGENTS.md`；
2. `README.rst`；
3. `project/STATE.rst`；
4. `project/DECISIONS.rst`；
5. `project/LEARNING_DESIGN.rst`；
6. `project/AIBOOK_LESSONS.rst`；
7. `project/CONTENT_SELECTION.rst`；
8. `project/REPOSITORY_MAP.rst`；
9. `manifests/tracks.toml`；
10. 当前路径与当前学习单元的状态文件；
11. 最近相关 RST 与 `main` 最近提交。

不得依赖上一段聊天的隐含记忆。仓库文件是跨对话、跨助手的交接依据。

## 4. 当前阶段

仓库处于学习模型校准阶段。

当前重点是从 `aiBook/docs` 选择三个典型失败案例，制作语言基础、系统行为和源码阅读三类黄金学习单元。黄金单元经实际阅读检查前，不批量迁移旧内容。

## 5. 分支规则

- 只在 `main` 分支工作；
- 不创建功能分支和 PR，除非用户明确改变规则；
- 每次工作围绕一个清晰目标；
- 不运行或配置 GitHub Actions。

## 6. 内容规划

学习路径开始时固定：

- 目标读者；
- 入口能力；
- 最终能力；
- 阶段顺序；
- 覆盖范围和排除项。

不在开头冻结整条路径的全部章节。每个阶段开始前设计一个小批次学习单元；每次处理当前批次中顺序最靠前的未完成项。批次完成后根据真实阅读反馈设计下一批。

## 7. 学习单元要求

一个正式学习单元至少需要形成以下主链：

1. 说明读者为什么现在会遇到这个问题；
2. 在解释前要求读者预测；
3. 给出最小代码、命令、输出、错误、界面现象或源码证据；
4. 逐步连接输入、调用、对象、状态和结果；
5. 改变至少一个条件并重新判断；
6. 指出一个常见错误模型及其错误原因；
7. 设计来自前述现象的能力检查；
8. 给出答案的推理过程，也说明常见错误答案为什么错；
9. 给出一个不能直接照抄原例子的迁移任务；
10. 用读者能独立完成的动作定义完成标准。

这是一条学习主链，不是强制标题模板。内容可以按主题自然组织，不能为了形式机械填满栏目。

## 8. RST 内容规则

- 一个文件只承担一次清晰的能力变化；
- 代码直接使用 `.. code-block::` 写在 RST 中；
- 命令和输出直接使用 `.. code-block:: console` 写在 RST 中；
- 源码级内容在当前 RST 中记录 repository、release、commit、平台和工具链；
- 不使用 `literalinclude`、Sphinx 专属交叉引用或依赖构建才能工作的结构；
- 使用普通 RST 相对链接连接文件；
- 不写宏大开场、重复总结、百科式分类和与正文脱节的问题；
- 不设置最低字数；能力变化完成后结束。

## 9. `aiBook` 的使用方式

`aiBook/docs` 只作为：

- 技术方向与主题库存；
- 已核实链接和源码路径的候选来源；
- 有教学价值的代码与失败案例来源；
- 旧教学结构的反面样本。

旧章节默认不直接改写。迁移顺序是：

`旧主题 → 真实困惑 → 能力变化 → 最小证据 → RST 学习单元 → 实际阅读检查`。

## 10. 工作结束记录

每次完成有意义的工作后，至少更新以下一项：

- `project/STATE.rst`：完成内容、当前问题和下一步；
- `project/DECISIONS.rst`：新的长期决定及原因；
- `project/REPOSITORY_MAP.rst`：文件职责变化；
- track 或 unit TOML：路径、阶段、单元和进度变化；
- 对应 RST：读者反馈、内容修订或完成状态。

后续对话必须只读取仓库即可继续工作。