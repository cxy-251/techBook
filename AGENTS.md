# AGENTS.md

## 仓库任务

`techBook` 当前只写 Linux Kernel 学习内容。

知识范围来自 `aiBook` 的 Linux Kernel Roadmap 和文章生成控制文件。Roadmap 表达用户希望获得的知识，不直接决定新仓库的章节数量和篇幅。技术事实重新依据官方文档、固定版本源码和可以确认的系统行为。

旧 `aiBook/docs/LinuxK` 只用于对比原内容为什么过长、前置不足或读完仍然无法形成判断。

## 当前工作

当前只处理一个学习单元：

``LK-001：程序输出一行文字时，为什么需要内核？``

它对应 Roadmap 开头“Linux 内核究竟管理什么”的知识需求。先完成这一篇，经过实际阅读和修改后，再决定下一个学习单元。

当前阶段聚焦 Linux Kernel，不扩展其他技术方向。

## 接手顺序

开始工作前依次阅读：

#. ``AGENTS.md``；
#. ``project/STATE.rst``；
#. ``project/LEARNING_DESIGN.rst``；
#. ``docs/tracks/linux-kernel/index.rst``；
#. 当前学习单元及其状态文件；
#. ``main`` 最近的相关提交。

仓库文件负责跨对话接续。

## 内容写法

每个学习单元只解决一个明确问题，并形成这条学习过程：

#. 读者先看到一个具体代码或系统现象；
#. 在解释前作出预测；
#. 观察代码、命令、输出或源码证据；
#. 沿输入、调用、对象、状态和结果逐步解释；
#. 改变一个条件并重新判断；
#. 指出常见错误理解；
#. 给出问题、答案推理和一个独立变化任务。

这些是学习步骤，不是必须逐项使用的标题模板。

代码、命令、输出和引用资料直接写在当前 RST 中。实际执行过的结果记录环境和日期；没有执行的结果标明“预期结果，尚未实测”。源码级结论记录使用的仓库、版本或 commit。

## 工作结束

完成内容或收到阅读反馈后，更新：

* ``project/STATE.rst``：当前做到哪里、发现了什么问题、下一步只做什么；
* 当前 Linux 路径或学习单元状态文件。
