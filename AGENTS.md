# AGENTS.md

## 当前任务

`techBook` 当前只写 Linux Kernel。

当前只完成一篇开篇正文：

``LK-BOOT-001：设备上电后，x86-64 Linux 内核怎样被装入并完成解压？``

固定软件主线：

``x86-64 → QEMU q35 → SeaBIOS → GRUB i386-pc → bzImage → Linux 6.12.95``。

正文从平台释放处理器复位开始，到压缩内核完成解压并跳入解压后的正式内核入口为止。当前文件稳定后，再沿实际控制流决定下一篇。

## 用户输入与技术事实

用户提供的内容用于表达兴趣、已知线索和阅读感受，不直接视为完整或正确的启动顺序。

Agent 必须：

* 先核对规范和固定版本源码；
* 纠正用户描述、旧 Roadmap 和现有正文中的错误；
* 补齐用户不知道或只用一句话概括的中间过程；
* 从当前入口继续追踪真实调用、状态变化和控制权交接；
* 不要求用户提前知道后续章节，也不等待用户为下一段提供情节。

下一篇由当前正文的自然终点和真实执行路径决定，不提前规划整本书。

## 内容依据

``aiBook`` 的 Linux Kernel Roadmap 只用于确认希望掌握的知识范围。旧 Roadmap 把世界观、阅读方法、源码导航、内核 C 和构建系统放在启动链之前，真正的固件、bootloader 和内核解压直到 Part 5 才出现；``techBook`` 不沿用这个开篇顺序。

当前正文使用：

* SeaBIOS 固定源码；
* GRUB i386-pc 固定源码；
* Linux 6.12.95 固定源码；
* Linux/x86 Boot Protocol；
* 能从源码文件、函数、CPU 模式、寄存器和内存布局确认的状态变化。

旧 ``aiBook/docs/LinuxK`` 只用于对比原内容在哪里绕远、重复或缺少关键细节。

## 正文写法

正文按时间顺序连续叙述，像追踪一次真实启动：

* 当前是谁在执行；
* CPU 处于什么模式；
* 代码和数据位于哪里；
* 当前完成了什么初始化；
* 下一次控制权跳转到哪个入口；
* 对应哪个源码文件、符号、寄存器或协议字段。

不能用“固件初始化硬件”“GRUB 加载内核”这样的概括跳过中间主流程。概念在流程第一次需要时直接解释，不单独讲方法论。

## 接手顺序

开始工作前依次阅读：

#. ``AGENTS.md``；
#. ``project/STATE.rst``；
#. ``docs/tracks/linux-kernel/index.rst``；
#. ``docs/tracks/linux-kernel/01-power-on-to-decompression.rst``；
#. ``manifests/tracks/linux-kernel.toml``；
#. ``main`` 最近的相关提交。

## 工作结束

完成正文或收到阅读反馈后，只更新当前正文、``project/STATE.rst`` 和 Linux 路径状态。