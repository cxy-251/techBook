# AGENTS.md

## 当前任务

`techBook` 当前只写 Linux Kernel。

这本书从设备上电后的故事引入，最终主题仍然是 Linux 内核。固件和 bootloader 属于内核取得控制权之前必须交代的前传，不单独扩展成硬件或固件教材。

当前只写第一章：

``LK-BOOT-001：按下电源键后，CPU 从哪里取得第一条指令？``

固定主线：

``x86-64 → QEMU q35 → SeaBIOS → GRUB i386-pc → bzImage → Linux 6.12.95``。

第一章结束在 SeaBIOS 的 reset vector 跳入固件正式入口。完成并经过阅读修改后，再沿同一时间线继续写下一章。

## 用户输入与技术事实

用户提供的是关注方向、已知线索和阅读感受，不直接作为完整或正确的启动顺序。

正文需要根据硬件规范、固件源码、启动协议、固定 Linux 源码和真实状态变化补全用户不知道的过程，并纠正不准确的前提。用户不知道后续流程时，Agent 继续沿当前控制流调查和写作，不要求用户提供下一段情节。

``aiBook`` 的 Linux Kernel Roadmap 只用于确认希望掌握的知识范围。旧 Roadmap 的章节顺序和篇幅不作为新书结构。

## 连续叙事

正文沿一条具体启动路径按实际发生顺序前进。每一段需要交代：

* 当前是谁在执行；
* CPU 处于什么状态或模式；
* 代码和关键数据位于哪里；
* 当前动作建立了什么条件；
* 控制权下一步交给哪个入口；
* 对应哪个规范、源码文件、符号、寄存器或协议字段。

不能用“固件初始化硬件”“GRUB 加载内核”这样的概括跳过中间主流程。概念在流程第一次需要时直接解释，不单独讲方法论。

## 章节边界

章节服务连续阅读，不按 Roadmap 条目机械切分，也不预先规划整本书。

当连续叙述已经形成适合一次阅读的篇幅，并且附近存在自然交接点时换章。自然交接点通常是：

* 执行者发生变化；
* CPU 模式或关键运行环境发生变化；
* 一项准备工作结束，下一项工作的目标明显改变；
* 控制权跳入新的入口、映像或源码区域。

篇幅只用于避免单章过长。不能为了达到字数截断一个尚未讲清的过程，也不能为了凑篇幅扩写背景。每章结尾明确记录当前执行者、当前状态和下一入口，使下一章可以直接接续。

## 当前内容依据

* x86-64 处理器复位状态与启动架构资料；
* SeaBIOS 固定源码；
* GRUB i386-pc 固定源码；
* Linux 6.12.95 固定源码；
* Linux/x86 Boot Protocol；
* 能从源码、寄存器、CPU 模式和内存布局确认的状态变化。

## 接手顺序

开始工作前依次读取：

#. ``AGENTS.md``；
#. ``project/STATE.rst``；
#. ``docs/tracks/linux-kernel/index.rst``；
#. ``docs/tracks/linux-kernel/01-power-on-first-instruction.rst``；
#. ``manifests/tracks/linux-kernel.toml``；
#. ``main`` 最近的相关提交。

## 工作结束

完成正文或收到阅读反馈后，只更新当前正文、``project/STATE.rst`` 和 Linux 路径状态。